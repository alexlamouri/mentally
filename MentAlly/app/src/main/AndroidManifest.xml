<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pacman.MentAlly">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.PHQ9.PHQ9ResultActivity"></activity>
        <activity android:name=".ui.PHQ9.PHQ9Activity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity android:name=".ui.PTSD.PTSDResultActivity" />
        <activity android:name=".ui.PTSD.PTSDActivity" />
        <activity android:name=".ui.PTSD.PTSD"/>


        <activity android:name=".ui.GAD7.GAD7ResultActivity" />
        <activity android:name=".ui.GAD7.GAD7Activity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="Mentally" />
        <activity
            android:name=".ui.register.RegisterActivity"
            android:label="@string/app_name" />
        <activity android:name=".ui.profile.ProfileActivity" />
    </application>

</manifest>